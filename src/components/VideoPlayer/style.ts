import styled from 'styled-components';

import { media } from '../../utils';

export const Wrapper = styled.section`
  position: absolute;
  inset: 0;
  width: 100%;
  min-height: 300px;
  aspect-ratio: 16 / 9;
  z-index: 1;
`;
