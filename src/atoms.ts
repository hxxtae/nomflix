import { atom } from 'recoil';

export const sliderLeave = atom({
  key: "sliderLeave",
  default: false
});

export const sliderLeaveP = atom({
  key: "sliderLeaveP",
  default: false
});

export const sliderLeaveT = atom({
  key: "sliderLeaveT",
  default: false
});

export const sliderLeaveU = atom({
  key: "sliderLeaveU",
  default: false
});
